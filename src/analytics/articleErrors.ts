export type ArticleErrorKind='invalid_parameters'|'unauthorized'|'database'|'timeout'|'internal';
export class ArticleAnalysisError extends Error { kind:ArticleErrorKind;status?:number;correlationId?:string;constructor(message:string,kind:ArticleErrorKind,status?:number,correlationId?:string){super(message);this.kind=kind;this.status=status;this.correlationId=correlationId} }
export interface ArticleErrorDetails {status?:number;code?:string;message?:string;correlationId?:string}
export const classifyArticleError=(details:ArticleErrorDetails)=>{const text=String(details.message||'').toLowerCase();const kind:ArticleErrorKind=details.status===400?'invalid_parameters':details.status===401||details.status===403?'unauthorized':details.status===504||text.includes('timeout')?'timeout':details.code==='DATABASE_ERROR'||details.status===503?'database':'internal';const labels={invalid_parameters:'Les filtres transmis sont invalides.',unauthorized:'Vous n’êtes pas autorisé à consulter cette analyse.',timeout:'Le délai de chargement a été dépassé.',database:'La base de données ne peut pas répondre pour le moment.',internal:'Une erreur interne empêche le chargement.'},reference=details.correlationId?` Référence : ${details.correlationId}.`:'';return new ArticleAnalysisError(labels[kind]+reference,kind,details.status,details.correlationId)};
export const isArticleFunctionUnavailable=(details:ArticleErrorDetails)=>details.status===404||details.code==='NOT_FOUND'||/function.*not found|not found.*function/i.test(details.message||'');
